package com.coffee.miniproject.repository;

import com.coffee.miniproject.model.BoardMain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardMainRepository extends JpaRepository<BoardMain, Integer> {
}
